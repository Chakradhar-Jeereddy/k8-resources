kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: roboshop-efs
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap             # Tells K8s to use access point and not root directory.
  fileSystemId: fs-0648de5d2e5cdfe42
  directoryPerms: "700"
  basePath: "/roboshop" # optional
